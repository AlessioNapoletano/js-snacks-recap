
const { createApp } = Vue;

createApp({
    data() {
        return {
            
        }
    },

    methods: {
        
    },

    created() {
        this.getRandomInvited();
    }
}).mount('#app')
